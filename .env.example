# Supabase project (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Site URL, used for OAuth redirects and links in notification emails
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Gmail SMTP (Google Workspace) — requires 2FA + an App Password
GMAIL_USER=dauren.oberhuber@yconnect.info
GMAIL_APP_PASSWORD=
